// Shared by the build hook and the runtime. These identifiers are independent
// of the file names/locations chosen by Dart or Flutter during packaging.
const sybdbAsset = 'package:mssql_native/sybdb';
const handlersAsset = 'package:mssql_native/handlers';
